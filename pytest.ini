[pytest]
# The existing .pytest_cache directory may be created as a file by older
# Windows test runs; use a dedicated writable cache location.
cache_dir = .pytest_cache_local
# Caching is unnecessary for this small suite and can fail on locked Windows
# worktrees where pytest cannot recreate its cache subdirectories.
addopts = -p no:cacheprovider
